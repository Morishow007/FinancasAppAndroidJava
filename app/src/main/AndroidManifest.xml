<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bruno.brunoepaulafinancas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BrunoEPaulaFinancas">
        <activity android:name=".activity.DespesasActivity" android:theme="@style/despesaTheme">
        </activity>
        <activity android:name=".activity.ReceitasActivity" android:theme="@style/receitaTheme" />
        <activity
            android:name=".activity.PrincipalActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.BrunoEPaulaFinancas"/>
        <activity android:name=".activity.CadastroActiviy" />
        <activity android:name=".activity.LoginActitivy" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>